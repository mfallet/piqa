#include <stdio.h>
#include <stdlib.h>
#include "debug.h"
#include "funcs.h"

int main(){
  int a = 0;
  int x = 0;
  int y = 0;
  int z = 0;

  z = x + y + a;
 
  printf("Variable z is set to %i. \n", z);
}
